# The secrets rules' fixture file must contain provider-key-shaped strings —
# that's what the rules detect. All keys in it are fake. Named explicitly
# (no glob) so future fixtures don't silently inherit the exemption.
paths-ignore:
  - "rules/secrets/provider-api-keys.py"
